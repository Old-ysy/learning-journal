# 个人习惯打卡器 · PRD

> Day 4 产品需求文档｜基于 Day 3 research.md 展开
> 作者：Old-ysy｜日期：2026-09-19

---

## 1. 项目概述

**一句话**：一个纯本地的习惯打卡工具，只让用户专注两件事——"今天做了什么"和"已经坚持了 N 天"。

**要解决的问题**（来自 research.md 第 1 节）：
想坚持的习惯总在"明天再说"里死循环；现有工具要么太花哨让人分心，要么太简陋让人感受不到积累。

**本期目标**：Day 7 前交付一个能日常使用的 Web MVP（单页、无后端、数据存浏览器）。

---

## 2. 目标用户

- **主要**：我自己（单用户，无登录）
- **未来**（不在本期）：2-3 人小圈子共用

用户特征：想坚持 3-5 个习惯但经常忘；讨厌复杂操作；手机和电脑都会用。

---

## 3. 页面结构

**只有一个页面**（单页应用，无路由）：

```
┌─────────────────────────────────┐
│  标题：今日打卡     日期（今天）   │
├─────────────────────────────────┤
│  输入框 [新增习惯名称]  [+ 添加]  │
├─────────────────────────────────┤
│  习惯列表                        │
│  ☐ 跑步 30 分钟      🔥 5 天    │
│  ☑ 读书 20 分钟      🔥 12 天   │
│  ☐ ...                          │
├─────────────────────────────────┤
│  （无习惯时显示空状态引导文案）     │
└─────────────────────────────────┘
```

---

## 4. 功能需求（本期做）

### F1 添加习惯
- 用户在输入框输入习惯名称（1-20 字符），点"添加"或按回车，习惯出现在列表顶部
- **上限 12 个**（沿用 Streaks 的"硬上限"设计，见 research.md 第 2 节；超过时提示"最多 12 个，先删一个吧"）
- 空输入不响应

### F2 勾选完成 / 撤销
- 点击习惯行的复选框：未勾 → 已勾（记录今天完成）；再点一次撤销
- 已完成状态当天刷新页面后保持

### F3 连续天数（streak）计算
- 每个习惯显示连续完成天数
- **计算规则**：按自然日（本地时区 0 点切换）。从今天往前数，连续每天都有完成记录则为 N 天；中间断了一天则重新从 0 计
- 今天已完成 → 显示当前 streak；今天还没勾但昨天及以前连续 → 显示昨天为止的 streak（不归零，给用户"今天还没做"的紧迫感而不是惩罚）

### F4 删除习惯
- 每行有删除按钮，点击弹浏览器原生 confirm 确认，确认后删除该习惯及其全部记录

### F5 数据持久化
- 所有数据（习惯列表 + 每日完成记录）存 localStorage
- 刷新、关浏览器重开不丢

### F6 空状态
- 列表为空时显示引导文案："还没有习惯，从第一个开始吧"

---

## 5. 非功能需求

- **响应式**：手机宽度（375px）到桌面（1440px）可正常使用
- **技术约束**：纯 HTML + CSS + 原生 JavaScript，无框架、无 CDN、无构建工具（Day 5 选型时遵守）
- **性能**：首屏本地文件直开，无网络请求
- **浏览器**：Chrome / Edge 最新两个大版本

---

## 6. 验收标准（Day 7 逐条测）

| # | 场景 | 预期 |
|---|---|---|
| A1 | 输入"跑步"回车 | 列表顶部出现"跑步"，streak 显示 0 天 |
| A2 | 输入空内容点添加 | 无反应，无报错 |
| A3 | 添加第 13 个习惯 | 弹提示"最多 12 个"，不新增 |
| A4 | 勾选"跑步" | 复选框变选中态，streak 变 1 天 |
| A5 | 再点一次取消勾选 | 恢复未选，streak 回 0 |
| A6 | 勾选后刷新页面 | 仍是已勾选状态 |
| A7 | 添加→关闭浏览器→重开 | 习惯和勾选状态都在 |
| A8 | 连续 3 天勾选（可改系统日期模拟） | streak 显示 3 天 |
| A9 | 中间隔一天没勾 | streak 从断点重新计数 |
| A10 | 点删除按钮 | 出现确认框；确认后行消失且刷新后不再出现 |
| A11 | 手机宽度（375px）打开 | 布局不错位，可正常操作 |
| A12 | localStorage 数据结构 | 纯 JSON，控制台可直接读取 |

---

## 7. 本期不做（继承 research.md 第 6 节）

账号登录、云端同步、通知提醒、统计图表、社交、习惯分类、多种频率、原生 App、主题切换、数据导出、备注心情、补打卡——**全部不做**。

---

## 8. 我在 PRD 里砍掉了什么（今日一问素材）

Day 3 调研时曾想放进 MVP、写 PRD 时砍掉的：

1. **习惯上限设 12 而不是不设限**——不是砍功能，是砍"自由度"
2. **砍掉"补打卡"**——本来想加"昨天忘了可以补"，砍掉是因为它会给"断了就放弃"的心理留后门，违背产品初衷
3. **砍掉 streak 的"宽容模式"**（漏一天不归零）——宽容会让数字失去意义
4. **砍掉热力图**——Loop 的热力图很好，但 MVP 先用数字，图表是 Day 30+ 的事

---

## 9. 后续推进

- Day 5：技术选型（实际就是确认第 5 节的约束）+ HTML 骨架
- Day 6：CSS + 交互逻辑
- Day 7：完成 F1-F6，按第 6 节验收标准自测
- Day 23：localStorage → 真实数据库

---

> 本文档由 Day 4 撰写，AI 已按第 6 节验收标准自检（见 commit 说明）。

# AGENTS.md｜项目协作规则

> 本文件是给 AI 助手（和我自己）的行为规则。
> Day 6 重建：Day 1 原文件未随 workspace 迁移（本项目自 Day 2 起在此环境开发），
> 按当日打卡任务「卡住降级」方案，先恢复 Day 1 基础规则，再追加 Day 6 新规则。

---

## 一、基础规则（Day 1）

1. **当天只做当天任务**，学员没说「进入下一板块」就不越界。
   不提前写明天的代码，不回头大改前几天的交付物。

2. AI 代办的每一步操作，都要说清楚做了什么、结果如何，让学员能看懂和学习。

3. commit 标题以「Day N｜」开头，说明写两行：改了什么 / 加了什么。

4. 每天收尾时：列出改动文件清单 → 标注各属于哪天的任务 → 提交推送 → 停下等确认。

---

## 二、追加规则（Day 6）

### R1：push 前必须亲眼检查暂存区
> 每次 `git commit` 之前，必须先跑 `git status --short` 和 `git diff --cached --stat`，
> 确认要提交的文件**每一个都是当天该提交的**，才能 commit。

**防的情况**：Day 2 实测发现 `.gitignore` 曾因内联注释写法失效，`git add .env` 一度成功。
如果当时没做 `git check-ignore` 实测就 push，密钥就裸奔到公开仓库了。
这条规则防的是「`git add .` 一把梭，把不该传的传上去」。

### R2：token / 密钥永不落盘
> PAT 等凭证只允许临时拼在 remote URL 里用一次，push 完成后**立刻**重置回干净 URL，
> 并用 `grep ghp_ .git/config` 验证无残留。凭证永不写进代码、commit、文档或记忆文件。

**防的情况**：凭证被写进文件 → 被 commit → 被 push 到公开仓库。
一旦发生，撤销 token 之外还要清洗 git 历史，成本极高。预防远比补救便宜。

### R3：push 成功必须以哈希一致为准
> 「推送完成」的判定标准是：`git rev-parse HEAD` 与 `git ls-remote origin main`
> 返回**同一个哈希**。任何一环（本地 commit、网络、远端状态）没对上就不算完成。

**防的情况**：网络抖动导致 push 失败但看起来「没报错」，或本地以为推了实际没推。
Day 5 就发生过 push 连续 2 次超时、第 3 次才成功——不核对哈希就会漏提交。

### R4：数据结构变更先改 TECH_DESIGN.md
> 任何要改动 localStorage schema（或未来的数据库结构）的代码，
> 必须先更新 TECH_DESIGN.md 第 3 节，再写代码。文档与实现不允许分叉。

**防的情况**：代码改了文档没改，几天后自己都不记得数据长什么样，
Day 23 换数据库时找不到「当时的约定」。

---

## 三、规则修改约定

- 只允许**追加**新规则或微调措辞，不删除已有规则
- 每条新规则必须写清楚「防的是哪种情况」——防不住具体问题的规则是废话
- 修改规则也算当天改动，须随当天 commit 一起提交
